---
version: "3"
services:
  devnetsandbox:
    build:
      context: .
      dockerfile: dockerfile
    image: howdareyou82/devnetsandbox
    volumes:
    - "$LOCALWORKDIR:/CODE:rw"
    network_mode: "bridge"
    container_name: dnsb